  function count_the_digit(n){

    let count = 0;

    while(n>0){
        count++
        
         n = Math.log10(n);

        // u Can do by using Math.trunc to remove fractional digits

        // n = Math.trunc(n/10);
    }
console.log(n)
  }
  count_the_digit(805)